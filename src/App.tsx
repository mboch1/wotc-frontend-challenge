import Layout from 'Layout';
import TalentCalculator from 'pages/TalentCalculator';

// in more complex app this would be a router with routes going to pages
const App = () => (
  <Layout>
    <TalentCalculator />
  </Layout>
);

export default App;